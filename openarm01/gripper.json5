{
  // This same interface is shared between the real robot and the sim implementations
  peppy_schema: "interface_v1",
  manifest: {
    name: "openarm01_gripper",
    tag: "0.1.0"
  },
  interfaces: {
    actions: [
      {
        name: "move_gripper",
        goal_service: {
          request_message_format: {
            // The frequency in hertz at which the feedback is being sent
            feedback_frequency: "u32",
            // Target gripper opening in meters (0.0 = closed, 0.044 = fully open on V10).
            position: "f64"
          },
          response_message_format: {
            accepted: "bool"
          }
        },
        // Streamed as the gripper moves toward the goal
        feedback_topic: {
          qos_profile: "standard",
          message_format: {
            joint_positions: {
              $type: "array",
              $items: "f64"
            },
            action_time: "f64"
          }
        },
        result_service: {
          response_message_format: {
            success: "bool",
            message: "string",
            final_joint_positions: {
              $type: "array",
              $items: "f64"
            },
            action_time: "f64"
          }
        }
      }
    ],
    services: [
      {
        // Returns the runtime gripper_id set in `execution.parameters` (allows `openarm01_backbone` to differentiate between the grippers)
        name: "get_gripper_id",
        response_message_format: {
          gripper_id: "u8"
        }
      }
    ]
  }
}